apiVersion: v1
kind: Service
metadata:
  name: {{ include "docfind.api.fullname" . }}
  labels:
    {{- include "docfind.api.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "docfind.api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: http
