apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "docfind.api.fullname" . }}-config
  labels:
    {{- include "docfind.api.labels" . | nindent 4 }}
data:
  app.yml: |
    {{- toYaml .Values.api.config | nindent 4 }}
